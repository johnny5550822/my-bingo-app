{
  "name": "bingo-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "static:serve": "npx serve dist -l 5000",
    "docker:build": "docker build -t bingo-generator .",
    "docker:run": "docker run --rm -p 5000:80 bingo-generator"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
